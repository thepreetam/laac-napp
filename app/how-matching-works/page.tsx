import { ArrowRight, CheckCircle2 } from 'lucide-react'
import Link from 'next/link'

const steps = ['Build a profile around your practice interests, geography, language skills, and bar timeline.', 'We compare those signals against active legal-aid roles and fellowships.', 'You see the reasons behind every suggested match and decide whether to express interest.']

export default function HowMatchingWorksPage() {
  return <div className="mx-auto max-w-4xl px-6 py-16"><p className="font-mono text-xs uppercase tracking-[0.12em] text-gold-deep">Transparent matching</p><h1 className="mt-2 font-display text-4xl font-semibold text-ink sm:text-6xl">A clearer path into public-interest law.</h1><p className="mt-5 max-w-2xl text-lg leading-relaxed text-ink-soft">LAAC Pipeline helps candidates find roles that fit their goals. It does not make decisions for employers or applicants; it makes the signal easier to understand.</p><ol className="mt-12 flex flex-col gap-5">{steps.map((step, index) => <li key={step} className="flex gap-4 rounded-md border border-line bg-card p-5"><span className="flex size-8 shrink-0 items-center justify-center rounded-full bg-sea font-mono text-sm text-paper">{index + 1}</span><p className="leading-relaxed text-ink">{step}</p></li>)}</ol><div className="mt-10 flex flex-wrap gap-4"><Link href="/onboarding/student" className="inline-flex items-center gap-2 rounded-md bg-sea px-5 py-3 font-medium text-paper hover:bg-sea-bright">Build your profile <ArrowRight className="size-4" /></Link><Link href="/fellowships" className="inline-flex items-center gap-2 rounded-md border border-line px-5 py-3 font-medium text-ink hover:border-sea">Explore pathways</Link></div><div className="mt-14 flex items-start gap-3 border-t border-line pt-6 text-sm text-ink-soft"><CheckCircle2 className="mt-0.5 size-4 shrink-0 text-sea" /><p>We show the matching reasons in plain sentences, so you can make an informed choice.</p></div></div>
}
